# experiment params
sample_size: 1 # number of NNs to train on each category

# files
filepaths:
  datasets: outputs/dev/datasets
  learning_results: outputs/dev/learning_results.npz

# network hyperparams
num_epochs: 400
batch_size: 8
learning_rate: 1e-3

# network architecture
